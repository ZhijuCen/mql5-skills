# SignalUnsubscribe

Cancels subscription.

```
bool  SignalUnsubscribe();

```

Return Value

Returns true if subscription has been canceled successfully, otherwise returns false. To read more about the [error](/en/docs/constants/errorswarnings/errorcodes) call [GetLastError()](/en/docs/check/getlasterror).
