# Build

Gets the build number of the client terminal.

```
int  CBuild() const 

```

Return Value

Build number of the client terminal.

Note

The terminal build number is defined by [TerminalInfoInteger()](/en/docs/check/terminalinfointeger) function ([TERMINAL_BUILD](/en/docs/constants/environment_state/terminalstatus#enum_terminal_info_integer) property).
