# PriceField

Returns the object (Low/High or Close/Close) to apply.

```
ENUM_STO_PRICE  PriceField() const

```

Return Value

The object (Low/High or Close/Close) to apply, defined at the indicator creation ([ENUM_STO_PRICE](/en/docs/constants/indicatorconstants/prices) enumeration value).
