# WindowHandle

Gets window handle of the chart (HWND).

```
int  WindowHandle() const

```

Return Value

Window handle of the chart assigned to the chart instance. If there is no chart assigned, it returns [INVALID_HANDLE](/en/docs/constants/namedconstants/otherconstants).
