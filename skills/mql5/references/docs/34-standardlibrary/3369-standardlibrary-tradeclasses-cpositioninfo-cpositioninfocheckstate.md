# CheckState

Checks the current parameters against the saved parameters.

```
bool  CheckState()

```

Return Value

true - the position parameters have changed since the last call of the [StoreState()](/en/docs/standardlibrary/tradeclasses/cpositioninfo/cpositioninfostorestate) method, otherwise - false.
