# Path

Gets the client terminal folder.

```
string  Path() const 

```

Return Value

The client terminal folder.

Note

The client terminal folder is used by [TerminalInfoString()](/en/docs/check/terminalinfostring) function ([TERMINAL_PATH](/en/docs/constants/environment_state/terminalstatus#enum_terminal_info_integer) property).
