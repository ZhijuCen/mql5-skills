# Applied

Returns the object (volume type) to apply.

```
ENUM_APPLIED_VOLUME  Applied() const

```

Return Value

Object (volume type) to apply, defined at the indicator creation ([ENUM_APPLIED_VOLUME](/en/docs/constants/indicatorconstants/prices) enumeration value).
